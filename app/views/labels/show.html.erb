<div class="link center">
  <p id="notice"><%= notice %></p>

  <p>
    <strong>Name:</strong>
    <%= @label.name %>
  </p>

  <%= link_to t('helpers.select.edit'), edit_label_path(@label) %> |
  <%= link_to t('helpers.select.back'), labels_path %>
</div>
